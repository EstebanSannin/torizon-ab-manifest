# Pinned upstream manifest — regenerated by scripts/update-upstream.sh <rev>.
#
# scarthgap-7.x.y = Torizon OS 7.x (Scarthgap). master will become the next
# release (wrynose / Torizon OS 8.x) — do NOT track it here yet.
# Release channel stays on a pinned REV; the nightly channel re-vendors at tip.
UPSTREAM_URL="https://git.toradex.com/toradex-manifest.git"
UPSTREAM_BRANCH="scarthgap-7.x.y"
UPSTREAM_REV="c8749217b706b091eb2f8560300ed56d5ff99774"
